// Define workplace types
export type Workplace = {
    id: string;
    name: string;
    address: string;
    latitude?: number;
    longitude?: number;
    // Add other workplace properties as needed
};
